"""
    # Extract the table class 
    tables = re.findall(r"(\w+)\.", source)
    unique_class_names = set([match for match in tables])
    assert len(unique_class_names) == 1, "The lambda expression must reference only one class"
    table = unique_class_names.pop()

    # Get the class definition
    table = globals()[table]
    print(table)

    # Find all the variables in the lambda expression
    variables = re.findall(r"\b\w+\b", source)
    unique_variables = set([match for match in variables])
    print(unique_variables)


    # Create the query string
    query = "SELECT * FROM Car WHERE "
    
    print(source)
"""